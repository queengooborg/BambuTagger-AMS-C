; ============================================================
;  BambuTagger-AMS-C — ESP32 Multi-Spool NFC Tag Reader
; ============================================================
[env:esp32-ams-c]
platform  = espressif32
board     = esp32dev
framework = arduino

; ── Chip / Flash ──────────────────────────────────────────
board_build.f_cpu       = 240000000L
board_build.f_flash     = 80000000L
board_build.flash_mode  = qio
; Project doesn't use SPIFFS/LittleFS, so trade filesystem space for a much
; larger OTA app partition (~1.9MB vs default ~1.3MB) to avoid overflow as
; the toolchain/Arduino core grows over time.
board_build.partitions  = min_spiffs.csv

; ── Serial / Upload ───────────────────────────────────────
monitor_speed = 115200
upload_speed  = 460800

; ── Build flags ───────────────────────────────────────────
build_flags =
    -DCORE_DEBUG_LEVEL=0
    -I src/display
    -I src/bambu
    -I src/ui

; ── Libraries ─────────────────────────────────────────────
lib_deps =
    https://github.com/MakerSpaceLeiden/rfid.git
    adafruit/Adafruit NeoPixel
    adafruit/Adafruit GFX Library
    adafruit/Adafruit ST7735 and ST7789 Library
    knolleary/PubSubClient
    bblanchon/ArduinoJson @ ^6.21.5
    adafruit/Adafruit BME280 Library
